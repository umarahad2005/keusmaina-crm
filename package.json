{
  "name": "keusmania-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Karwan-e-Usmania CRM — monorepo root. client/ deploys to Vercel, server/ to Render; neither builds from here.",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "npm install --prefix client && npm install --prefix server"
  }
}
